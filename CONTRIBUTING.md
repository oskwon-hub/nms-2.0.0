# 기여 가이드 (Contributing)

이 프로젝트에 관심을 가져 주셔서 감사합니다. 버그 리포트, 기능 제안, 코드 기여 모두
환영합니다.

## 이슈 등록

- 버그를 발견했다면 재현 방법, 기대 동작, 실제 동작, 환경(OS/Python/Node 버전 등)을
  포함해 이슈를 등록해 주세요.
- 기능 제안은 해결하려는 문제와 제안하는 접근 방식을 함께 설명해 주시면 논의가
  빨라집니다.
- 보안 취약점은 공개 이슈 대신 [SECURITY.md](SECURITY.md)의 절차를 따라 주세요.

## 개발 환경 준비

[README.md](README.md#빠른-시작)의 "빠른 시작"을 참고해 백엔드/프런트엔드 개발 환경을
준비합니다.

## 변경 사항 제출 절차 (Pull Request)

1. 저장소를 Fork하고, 작업용 브랜치를 새로 만듭니다(`main`에서 바로 작업하지 않습니다).
2. 변경은 가능한 한 작은 단위로 나눠서 커밋합니다. 커밋 메시지는 "무엇을" 보다 "왜"
   바꿨는지가 드러나도록 작성해 주세요.
3. 코드를 변경했다면 관련 테스트를 추가/수정하고, 아래 검증을 모두 통과하는지
   확인합니다.
   - 백엔드: `cd backend && .venv/bin/python -m pytest -q`
   - 프런트엔드: `cd frontend && npx tsc --noEmit -p tsconfig.json && npx vite build`
4. PR 설명에는 변경 배경, 주요 변경 내용, 테스트 결과(명령과 결과)를 포함해 주세요.
5. 리뷰 코멘트에는 성실하게 응답해 주시고, 합의된 변경 사항을 반영해 주세요.

## 코드 스타일

- 새로운 로직이나 예외 처리를 추가할 때, 그 이유(WHY)가 코드만으로는 명확하지 않다면
  간단한 주석을 남겨 주세요. 무엇을 하는지(WHAT)는 가능한 한 함수/변수 이름으로
  드러내고, 주석은 배경/제약/트레이드오프 설명에 집중해 주세요.
- 기존 코드 스타일(들여쓰기, 네이밍 규칙 등)을 따라 주세요. 별도의 포매터/린터 설정은
  각 하위 프로젝트(`backend`, `frontend`)의 설정 파일을 기준으로 합니다.
- 요청받은 범위를 벗어나는 리팩터링은 별도 PR로 분리해 주세요. 하나의 PR은 하나의
  목적에 집중하는 것이 리뷰에 도움이 됩니다.

## 라이선스 동의

이 저장소에 기여하는 코드는 [GPLv2 라이선스](LICENSE)로 배포됩니다. PR을 제출함으로써
기여하신 코드가 이 라이선스로 배포되는 데 동의하는 것으로 간주합니다.

# 보안 정책 (Security Policy)

## 지원 버전

현재 `main` 브랜치 기준 최신 버전만 보안 패치를 지원합니다.

| 버전 | 지원 여부 |
| --- | --- |
| 2.0.x | ✅ |
| 1.x (nms-1.3.0 등 이전 세대) | ❌ |

## 취약점 제보 방법

이 프로젝트는 네트워크 장비에 SNMP/SSH로 접근하고 자격증명을 암호화 저장하는 등
보안에 민감한 기능을 다룹니다. 취약점을 발견하셨다면 **공개 이슈로 먼저 등록하지 마시고**
아래 방법 중 하나로 비공개 제보해 주세요.

- GitHub을 사용 중이라면 저장소의 **Security 탭 → "Report a vulnerability"**
  (Private Vulnerability Reporting)를 이용해 주세요.
- 위 기능을 사용할 수 없다면, 저장소 관리자에게 비공개 채널(예: 저장소 정보에 등록된
  관리자 연락처)로 직접 연락해 주세요.

제보 시 다음 정보를 포함해 주시면 대응이 빨라집니다.

- 취약점 종류(예: 인증 우회, 명령 삽입, 자격증명 노출 등)와 영향 범위
- 재현 절차 또는 PoC
- 영향받는 버전/커밋

## 대응 절차

1. 제보 접수 후 가능한 빠르게(통상 5영업일 이내) 확인 회신을 드립니다.
2. 심각도를 평가하고 수정 계획을 공유합니다.
3. 패치가 준비되면 새 릴리스를 배포하고, 제보자 동의 하에 크레딧을 남깁니다.
4. 패치 배포 전까지는 취약점 세부 내용을 비공개로 유지해 주시길 부탁드립니다
   (Coordinated Disclosure).

## 알려진 보안 관련 설계 사항

- SNMP community, SSH 계정 등 장비 자격증명은 평문으로 DB에 저장하지 않고
  `backend/app/security.py`의 암/복호화 로직을 거쳐 저장합니다. 복호화 키
  (`backend/data/secret.key`, 배포 시 생성)는 저장소에 포함하지 않으며 각 배포 환경에서
  별도로 관리해야 합니다.
- 포트 Shutdown, PoE 제어 등 장비에 실제 변경을 가하는 기능은 `backend/app/control/protected.py`
  기준으로 보호 대상 장비/포트를 제외하도록 설계되어 있습니다. 운영 환경에 배포하기 전
  보호 목록 설정을 반드시 검토해 주세요.

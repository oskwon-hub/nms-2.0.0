# Claude 작업 규칙

코드 작업 시 다음 절차를 따른다.

1. 관련 파일을 먼저 분석한다.
2. 필요한 파일만 수정한다.
3. 작업 후 테스트를 실행한다.
4. 테스트 실패나 미해결 문제를 숨기지 않는다.
5. 작업 완료 후 `docs/ai-log/<작업일자 YYYY-MM-DD>.md`에 작업 내용을 기록한다 (날짜별로 별도 파일. 같은 날짜에 여러 작업을 하면 같은 파일에 섹션을 추가한다).
6. Git 커밋은 사용자의 명시적인 요청이 있을 때만 수행한다.

`docs/ai-log/<날짜>.md`에는 다음 항목을 추가한다.

* 작업 일자
* 작업명
* 사용자 요청 요약
* 변경 파일
* 주요 변경 내용
* 테스트 명령과 결과
* 미해결 사항
* 추천 Git 커밋 메시지

코드에 새로운 로직/예외 처리를 추가할 때, 그 이유를 설명하는 주석 맨 앞에 `[KOS<작업일자 YYYYMMDD>]` 태그를 붙인다 (예: `# [KOS20260714] ...`). WHY가 명확하지 않은 곳에만 주석을 달되, 달 때는 이 태그로 시작한다.
